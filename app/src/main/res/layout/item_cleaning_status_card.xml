<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_cleaning_status"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/card_background"
    app:rippleColor="@color/primary">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Cleaning Icon -->
        <TextView
            android:id="@+id/icon_cleaning"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🧹"
            android:textSize="24sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Cleaning Title -->
        <TextView
            android:id="@+id/text_cleaning_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:text="@string/cleaning_status_title"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            android:textColor="@color/text_primary"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/icon_cleaning"
            app:layout_constraintTop_toTopOf="@id/icon_cleaning"
            app:layout_constraintEnd_toStartOf="@id/text_status_badge"
            app:layout_constraintBottom_toBottomOf="@id/icon_cleaning" />

        <!-- Status Badge -->
        <TextView
            android:id="@+id/text_status_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textStyle="bold"
            android:background="@drawable/status_badge_background"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Pending"
            tools:textColor="@color/warning_text" />

        <!-- Divider -->
        <View
            android:id="@+id/divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:background="@color/divider"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/icon_cleaning" />

        <!-- Scheduled Time Section -->
        <LinearLayout
            android:id="@+id/layout_scheduled_time"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/divider">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_schedule"
                android:contentDescription="@string/cleaning_scheduled_time"
                app:tint="@color/text_secondary" />

            <TextView
                android:id="@+id/text_scheduled_time"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="@color/text_secondary"
                tools:text="Daily at 3:54 PM" />

            <!-- Mark Clean Button (shown when there's a pending cleaning) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_confirm_cleaning"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Mark Clean"
                android:textSize="12sp"
                android:visibility="gone"
                app:cornerRadius="18dp"
                app:icon="@drawable/ic_check_circle"
                app:iconSize="16dp"
                app:iconTint="@color/normal_text"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                tools:visibility="visible" />

        </LinearLayout>

        <!-- No Schedule Message (shown when no schedule is set) -->
        <TextView
            android:id="@+id/text_no_schedule"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/cleaning_no_schedule"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="@color/text_secondary"
            android:textStyle="italic"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/divider"
            tools:visibility="gone" />

        <!-- Overdue Warning Banner (shown when overdue) -->
        <LinearLayout
            android:id="@+id/layout_overdue_warning"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@color/critical_background"
            android:padding="10dp"
            android:gravity="center_vertical"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/layout_scheduled_time"
            tools:visibility="visible">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_warning"
                android:contentDescription="@string/warning"
                app:tint="@color/critical_text" />

            <TextView
                android:id="@+id/text_overdue_message"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="@color/critical_text"
                android:textStyle="bold"
                tools:text="Cleaning overdue by 45 minutes!" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
